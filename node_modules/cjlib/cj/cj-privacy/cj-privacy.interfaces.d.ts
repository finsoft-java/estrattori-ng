import { Observable } from 'rxjs/Observable';
import { ICjRestOutput } from '../cj-commons/cj-commons.interface';
/**
 * Struttura dati relativa al dettaglio di privacy
 */
export interface ICjDettaglioPrivacy {
    note?: string;
    consenso1?: boolean;
    consenso2?: boolean;
    consenso3?: boolean;
    consenso4?: boolean;
    consenso5?: boolean;
    consenso6?: boolean;
    dataConsenso1?: string;
    dataConsenso2?: string;
    dataConsenso3?: string;
    dataConsenso4?: string;
    dataConsenso5?: string;
    dataConsenso6?: string;
    dataInvInf?: string;
    sportRif?: string;
    rifer?: string;
}
/**
 * Servizio utilizzato per recuperare le informazioni di privacy
 *
 */
export interface ICjPrivacyService {
    getPrivacy(ndg: string, abi: string, unitaOperativa: string): Observable<ICjDettaglioPrivacy>;
    savePrivacy(data: ICjDettaglioPrivacy, ndg: string, abi: string, unitaOperativa: string): Observable<ICjDettaglioPrivacy>;
    printPrivacy(ndg: string, abi: string, unitaOperativa: string, tipoOfferta: 'OFS' | 'OIS' | 'OAD'): Observable<any>;
}
export interface IPrivacyUrlParams {
    ndgUrlParam: string;
    abiUrlParam: string;
    unitaOperativaUrlParam: string;
    tipoOffertaUrlParam: 'OFS' | 'OIS' | 'OAD';
}
export interface ICjPrintPrivacyData extends ICjRestOutput {
    xmlToPrint: string;
}
