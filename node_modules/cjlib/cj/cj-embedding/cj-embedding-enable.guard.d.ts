import { CanActivate, ActivatedRouteSnapshot, RouterStateSnapshot } from '@angular/router';
import { CjIFrameEmbeddingService } from './cj-iframe-embedding.service';
export declare class CjEmbeddingEnableGuard implements CanActivate {
    private iframeEmbeddingService;
    constructor(iframeEmbeddingService: CjIFrameEmbeddingService);
    canActivate(route: ActivatedRouteSnapshot, state: RouterStateSnapshot): boolean;
}
