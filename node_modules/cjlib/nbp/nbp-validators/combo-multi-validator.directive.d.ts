import { AbstractControl, RequiredValidator, ValidationErrors } from '@angular/forms';
export declare class ArrayRequiredValidator extends RequiredValidator {
    validate(c: AbstractControl): ValidationErrors | null;
}
