import { EventEmitter, OnInit } from '@angular/core';
import { NbpStyle } from './../nbp-commons/nbp-commons.enums';
/**
 * @export
 * @class NbpDividingLineComponent
 * @implements {OnInit}
 * Visualizza una linea di separazione, con un pulsante opzionale e un titolo opzionale
 */
export declare class NbpDividingLineComponent implements OnInit {
    /**
     * Identificativo univoco per il componente.
     * Se nessun id è fornito e' auto-generato.
     */
    id: string;
    /**
     * (Opzionale) Titolo visualizzato nella linea divisoria.
     */
    nbpTitle: string;
    /**
     * (Opzionale) Nome della classe icona da visualizzare nella linea divisoria
     */
    nbpTitleIcon: string;
    private _nbpShowButton;
    /**
     * (Opzionale) Booleano che mostra/nasconde il pulsante. Default: false
     */
    nbpShowButton: boolean;
    /**
     * (Opzionale) Testo visualizzato nel pulsante
     */
    nbpButtonLabel: string;
    /**
     * (Opzionale) Nome della classe icona da visualizzare nel pulsante
     */
    nbpButtonIcon: string;
    /**
     * (Opzionale) Nome della classe icona da visualizzare nel pulsante all'over
     *  Obbligatoria se presente nbpButtonIcon
     */
    nbpButtonIconHover: string;
    /**
     * (Opzionale) Indica quanto spazio deve occupare il braccio sinistro affinchè si accentri l'icona o la label in caso di presenza del pulsante laterale.
     */
    marginLeft: string;
    /**
     * (Opzionale) Stile della linea di separazione, di default è grigia.
     * Se impostata su NbpStyle.PRIMARY è di colore verde
     */
    nbpLineStyle: NbpStyle.PRIMARY | NbpStyle.DEFAULT;
    _lineStyle: any;
    /**
     * (Opzionale) Evento emesso al click sul pulsante.
     * @param {EventEmitter<any>} nbpClick
     */
    nbpClick: EventEmitter<any>;
    _onClick(): void;
    constructor();
    center: boolean;
    ngOnInit(): void;
}
