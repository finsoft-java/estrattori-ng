/**
 * Enum necessaria per la definizione del tipo di pulsante
 */
export declare enum NbpButtonType {
    ACTION = 0,
    SELECT = 1,
    GENERATIVE = 2,
    CHECK = 3,
    PROFILE = 4,
    GENERATIVEROUND = 5,
}
