import { PipeTransform } from '@angular/core';
export declare class NbpCurrencyPipe implements PipeTransform {
    private DECIMAL_SEPARATOR;
    private THOUSANDS_SEPARATOR;
    private THOUSANDS_SEPARATOR_REGEX;
    private DECIMAL_REGEX;
    private DECIMAL_VIEW_REGEX;
    constructor();
    inputIsValid(input: string): boolean;
    private viewInputIsValid(input);
    transform(value: number | string, fractionSize?: number): string;
    parse(value: string, fractionSize?: number): string;
}
