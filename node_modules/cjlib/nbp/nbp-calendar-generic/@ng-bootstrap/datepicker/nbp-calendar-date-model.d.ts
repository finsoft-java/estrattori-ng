import { NgbDateStruct } from './ngb-date-struct';
/**
 * Classe Model per l'inizializzazione del model del componente nbp-calendar-generic
 */
export declare class NbpCalendarDateModel implements NgbDateStruct {
    /**
     * The year, for example 2016
     */
    year: number;
    /**
     * The month, with default calendar we use ISO 8601: 1=Jan ... 12=Dec
     */
    month: number;
    /**
     * The day of month, starting at 1
     */
    day: number;
    constructor(d: Date | NgbDateStruct);
    /**
     * per verificare che implementi l'interface NgbDateStruct non posso usare instanceof,
     * quindi controllo esistano i campi al nuo interno e non siano nulli
     * @param d
     */
    instanceOfNgbDateStruct(d: any): boolean;
    isNill(value: any): boolean;
}
