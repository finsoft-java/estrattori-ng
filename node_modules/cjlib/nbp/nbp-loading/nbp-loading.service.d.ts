import { Observable } from 'rxjs/Observable';
import { INbpLoadingStatus } from './../nbp-commons/nbp-commons.interface';
import 'rxjs/add/operator/filter';
import 'rxjs/add/operator/windowWhen';
import 'rxjs/add/operator/map';
import 'rxjs/add/observable/timer';
/**
 * Servizio utilizzato per mantenere e recuperare lo stato del loadingSpinner
 */
export interface INbpLoadingService {
    notifyLoadingSpinnerRequest(showLoadingSpinner: boolean, id?: string): void;
    getLoadingSpinnerStatusStream(id?: string): Observable<INbpLoadingStatus>;
}
export declare class NbpLoadingService implements INbpLoadingService {
    /**
     * ReplaySubject -> manda a ogni subscriber anche se si registra dopo tutta la storia dello stream dall'inizio
     *
     * BehaviorSubject -> manda a ogni subscriver solo l'item più recente presente nello stream
     */
    constructor();
    private _loadingSpinnerRequestStream;
    private _loadingSpinnerAggregatedRequestStream;
    private statusArray;
    private handleMultipleActivationStatus(key);
    /**
     * Permette di inviare le richieste di attivazione spegnimento del componente loadingSpinner
     */
    notifyLoadingSpinnerRequest(showLoadingSpinner: boolean, id?: string): void;
    /**
     * Recupera lo stato aggregato (per id e multiple nexted activation) della visibilità del componente loadingSpinner
     */
    getLoadingSpinnerStatusStream(id?: string): Observable<INbpLoadingStatus>;
}
