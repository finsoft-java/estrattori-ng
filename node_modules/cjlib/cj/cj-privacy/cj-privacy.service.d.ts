import { Observable } from 'rxjs/Observable';
import { ICjDettaglioPrivacy, ICjPrivacyService, ICjPrintPrivacyData } from './../cj-privacy/cj-privacy.interfaces';
import { CjPrivacyClientService } from './cj-privacy-client.service';
import { ICjRestOutput } from '../cj-commons/cj-commons.interface';
/**
 * Servizio utilizzato per recuperare le informazioni di contesto
 *
 */
export declare class CjPrivacyService implements ICjPrivacyService {
    private cjPrivacyClient;
    constructor(cjPrivacyClient: CjPrivacyClientService);
    /**
     * Restituisce le informazioni di privacy a partire dall'ndg, chiamando il servizio rest dedicato.
     * @param  string ndg
     * @return ICjPrivacy
     */
    getPrivacy(ndg: string, abi: string, unitaOperativa: string): Observable<ICjDettaglioPrivacy>;
    /**
     * Salva i consensi privacy compilati tramite form.
     * @param  string ndg
     * @return ICjPrivacy
     */
    savePrivacy(data: ICjDettaglioPrivacy, ndg: string, abi: string, unitaOperativa: string): Observable<ICjRestOutput>;
    /**
     * Stampa i consensi privacy compilati tramite form.
     * @param  string ndg
     * @return any
     */
    printPrivacy(ndg: string, abi: string, unitaOperativa: string, tipoOfferta: 'OFS' | 'OIS' | 'OAD'): Observable<ICjPrintPrivacyData>;
}
