import { OnInit, EventEmitter } from '@angular/core';
import { FileUploader } from 'ng2-file-upload';
export declare class NbpUploadFileComponent implements OnInit {
    /**
     * Evento per passare all'esterno la lista dei file.
     */
    nbpUploadFile: EventEmitter<File[]>;
    /**
     * Evento per associare una funziona esterna all'icona della lista dei file. Ritorna l'oggetto del file selezionato.
     */
    nbpEventIcon: EventEmitter<File>;
    /**
     * Tipo di caricamento dei file. Può assumere due valori:
     * 'single': permette di selezionare un solo file;
     * 'multiple': permette di selezionare più file;
     */
    typeUpload: String;
    /**
     * Stringa che indica i tipi di file che possono essere caricati. Le tipologie sono separate da una virgola.
     * Esempio: 'application/pdf,image/jpeg,image/png,image/gif';
     */
    typeFile: string;
    /**
     * Label della sezione.
     */
    labelTitleDoc: string;
    uploader: FileUploader;
    hasBaseDropZoneOver: boolean;
    hasAnotherDropZoneOver: boolean;
    fileOverBase(e: any): void;
    fileOverAnother(e: any): void;
    files: any[];
    fileOverBox: boolean;
    changeListener($event: any): void;
    readThisDrop(inputDrop: any): void;
    iconFn(index: any): void;
    constructor();
    ngOnInit(): void;
    setAllowedFile(): any[];
    fileDropFn(e: any): void;
    removeFile(indice: any): void;
}
