import { OnChanges, SimpleChanges } from '@angular/core';
import { IClientError } from '../../utility/global-error-handler/global-error-handler.interfaces';
import { NbpStyle } from './../nbp-commons/nbp-commons.enums';
export declare class NbpDebugInfoComponent implements OnChanges {
    /**
     * Errore da visualizzare
     */
    nbpError: IClientError;
    _primary: NbpStyle;
    _copyMessage: string;
    _isErrorCopied: boolean;
    ngOnChanges(changes: SimpleChanges): void;
    copyText(event: any): void;
    constructor();
}
