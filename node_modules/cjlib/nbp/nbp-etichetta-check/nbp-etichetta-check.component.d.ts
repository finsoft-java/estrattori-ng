import { EventEmitter } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
/**
 * Componente etichetta dal funzionamento analogo a quello di una checkbox.
 */
export declare class NbpEtichettaCheckComponent implements ControlValueAccessor {
    /**
     * testo da inserire all'interno del bottone-filtro
     */
    key: string;
    /**
     * input che determina il valore quando il bottone è selezionato
     */
    trueValue: boolean;
    /**
    * input che determina il valore quando il bottone non è selezionato
    */
    falseValue: boolean;
    /**
     * evento per la rimozione del bottone
     */
    removeButtonEvt: EventEmitter<string>;
    /**
     * evento lanciato al cambiamento di stato del bottone
     */
    change: EventEmitter<any>;
    private checkValRef;
    /**
     * variabile booleana interna che identifica se il bottone/check è true o false
     */
    checked: Boolean;
    constructor();
    propagateChange: (_: any) => void;
    onTouchedCallback: () => {};
    writeValue(value: any): void;
    registerOnChange(fn: any): void;
    registerOnTouched(fn: any): void;
    /**
     *
     * funzione innescata da un cambiamento esterno del valore del bottone/check
     */
    onChange(event: any): void;
    _overIcon: boolean;
    /**
     * evento per la rimozione del bottone
     */
    removeButton: () => void;
}
