import { EventEmitter } from '@angular/core';
export declare class NbpAutoFocusInModalDirective {
    constructor();
    forcedFocusTabChange: EventEmitter<any>;
    /**
     * evento chiamato dalla modale (nbp-modal) all'apertura per simulare il focus via tab
     * @param event
     */
    setTabFocus(event: any): void;
}
