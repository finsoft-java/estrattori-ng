import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs/Observable';
import { ApplicationConfig } from '../../module.config';
import { ICjDettaglioPrivacy, ICjPrintPrivacyData } from '../cj-privacy/cj-privacy.interfaces';
import { ICjRestOutput } from '../cj-commons/cj-commons.interface';
import 'rxjs/add/operator/catch';
import 'rxjs/add/operator/map';
import 'rxjs/add/operator/toPromise';
/**
 * Client Http per recuperare le informazioni di privacy
 */
export declare class CjPrivacyClientService {
    private config;
    private http;
    private headers;
    constructor(config: ApplicationConfig, http: HttpClient);
    getPrivacyByNdg(ndg: string, abi: string, unitaOperativa: string): Observable<ICjDettaglioPrivacy>;
    savePrivacy(data: ICjDettaglioPrivacy, ndg: string, abi: string, unitaOperativa: string): Observable<ICjRestOutput>;
    printPrivacy(ndg: string, abi: string, unitaOperativa: string, tipoOfferta: 'OFS' | 'OIS' | 'OAD'): Observable<ICjPrintPrivacyData>;
}
