import { Validator, AbstractControl } from '@angular/forms';
import { NbpCustomValidationService } from './nbp-custom-validation.service';
export declare class CustomValidator implements Validator {
    private validationService;
    validationKeys: Array<string>;
    constructor(validationService: NbpCustomValidationService);
    validate(control: AbstractControl): {
        [key: string]: any;
    };
}
