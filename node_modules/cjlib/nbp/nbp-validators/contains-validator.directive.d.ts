import { Validator, AbstractControl } from '@angular/forms';
export declare class ContainsValidator implements Validator {
    validateContains: Array<any>;
    customValidationMessage: string;
    validate(control: AbstractControl): {
        [key: string]: any;
    };
}
