import { EventEmitter } from '@angular/core';
import { INbpPagingConfiguration } from './nbp-paging.interfaces';
export declare class NbpPagingComponent {
    /**
     * Proprietà che definisce un array di ImbpPagingConfiguration
     * necessario per gestire le singole pagine
     */
    nbpConfiguration: INbpPagingConfiguration;
    /**
     * evento generato al click sul pallino che indica la pagina.
     * Contiene il numero della pagina a cui si desidera tornare.
     */
    nbpGoToPage: EventEmitter<any>;
    constructor();
    /**
     * funzione invocata al click sul pallino che indica la pagina.
     */
    onClickPage(indice: Number): void;
}
