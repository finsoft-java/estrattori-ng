import { OnInit, EventEmitter } from '@angular/core';
import { INavigationStep, INavigationStepEvent } from './nbp-step-navigabili.interfaces';
export declare class NbpStepNavigabiliComponent implements OnInit {
    private document;
    stickyActive: boolean;
    /**
     * Booleano opzionale che indica se si voglia gestire o meno il comportamento sticky allo scroll.
     * Se false, il componente verrà nascosto allo scroll, altrimenti gli verrà assegnata una posizione fissa.
     */
    sticky: boolean;
    /**
     * Array di step. Ciascuno step possiede:
     * - name: string;
     * - selected: boolean;
     * - disabled: boolean;
     * - activated: boolean;
     * - disableClick?: boolean;
     * - onClick: any;
     */
    nbpSteps: Array<INavigationStep>;
    /**
     * Evento emesso all'avanzamento. Include indice (index) e step selezionato (data).
     */
    nbpOnStepClick: EventEmitter<INavigationStepEvent>;
    constructor(document: any);
    selectStep(index: number): void;
    onWindowScroll(): void;
    ngOnInit(): void;
}
