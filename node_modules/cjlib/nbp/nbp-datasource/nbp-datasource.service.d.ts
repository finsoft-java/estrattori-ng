import { INbpArrayDataSource, IDataPage } from './nbp-datasource.interfaces';
import { Observable } from 'rxjs/Observable';
import 'rxjs/add/operator/map';
import 'rxjs/add/observable/of';
export declare class NbpDataSource<T> extends INbpArrayDataSource<T> {
    private defaultPageSize;
    arrayData: Array<T>;
    constructor(arrayData?: Array<T>, defaultPageSize?: number);
    private getSortingDirections(sortFields);
    private dataSortComparator(sortDirection, sortField);
    private sortData(data, sortFields);
    getPage(pageNumber?: number, sorting?: string[], pageSize?: number): Observable<IDataPage<T>>;
    setData(newArrayData: Array<T>, pageNumber?: number, sortFields?: Array<string>, pageSize?: number): void;
}
