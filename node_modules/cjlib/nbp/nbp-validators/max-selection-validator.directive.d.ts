import { Validator, AbstractControl } from '@angular/forms';
export declare class MaxSelectionValidator implements Validator {
    maxSelection: string;
    validate(control: AbstractControl): {
        [key: string]: any;
    };
}
