import { OnInit, EventEmitter } from '@angular/core';
/**
 * Componente etichetta con possibilità di chiusura. Utile per rappresentare e rimuovere filtri.
 */
export declare class NbpEtichettaFiltroComponent implements OnInit {
    constructor();
    /**
     * valore in input della label del bottone
     */
    key: string;
    /**
     * evento in output al click sull'icona di rimozione
     */
    removeFilterEvt: EventEmitter<string>;
    ngOnInit(): void;
    removeFilter: () => void;
}
