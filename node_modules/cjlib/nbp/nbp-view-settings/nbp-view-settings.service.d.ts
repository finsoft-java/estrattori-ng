import { Observable } from 'rxjs/Observable';
import { NbpFontSize } from '../nbp-commons/nbp-commons.enums';
import 'rxjs/add/operator/filter';
import { NbpEmbeddingService } from './../nbp-embedding/nbp-embedding.service';
export declare class NbpViewSettingsService {
    private embeddingService;
    private _contrast$;
    private _fontSize$;
    constructor(embeddingService: NbpEmbeddingService);
    receiveEvents(): void;
    getPageHeight(): string;
    getContrastoEventStream(): Observable<boolean>;
    notifyContrastoEvent(isContrastoOn: boolean): void;
    getFontSizeEventStream(): Observable<string>;
    notifyFontSizeEvent(fontSize: NbpFontSize): void;
}
