import { OnInit, OnChanges, SimpleChanges, EventEmitter } from '@angular/core';
import { IRadioStructure } from './nbp-radio-selection.interfaces';
export declare class NbpRadioSelectionComponent implements OnInit, OnChanges {
    /**
    *  Lista dei dati visualizzati nel componente e selezionabili
    */
    nbpRadioList: Array<IRadioStructure>;
    /**
    * (Opzionale) Dati per popolare il componente
    */
    nbpData: string;
    /**
     * Evento emesso al click sui radio button
     * @param {EventEmitter<any>}  nbpOnSelection
     */
    nbpOnSelection: EventEmitter<string>;
    _radioList: Array<IRadioStructure>;
    constructor();
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    _onSelection(value: string): void;
    private _isNbpDataValid();
}
