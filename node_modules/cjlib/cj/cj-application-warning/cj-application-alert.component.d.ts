import { OnInit, OnDestroy, NgZone } from '@angular/core';
import { ApplicationStatusService } from './../../utility/application-status/application-status.service';
import { NbpAlertType } from './../../nbp/nbp-commons/nbp-commons.enums';
export interface IApplicationAlertData {
    type: NbpAlertType;
    content: string;
    show: boolean;
}
export declare class CjApplicationAlertComponent implements OnInit, OnDestroy {
    private applicationStatus;
    private zone;
    /**
     * (Opzionale) Array di alert applicativi che si pososno passare al componente
     * istanziato in pagina
     */
    cjApplicationAlerts: Array<IApplicationAlertData>;
    id: string;
    _alertContent: string;
    private _alertSubscription;
    constructor(applicationStatus: ApplicationStatusService, zone: NgZone);
    ngOnInit(): void;
    ngOnDestroy(): void;
    _onCloseAlert(alertIndex: number): void;
    closeAllAlert(): void;
}
