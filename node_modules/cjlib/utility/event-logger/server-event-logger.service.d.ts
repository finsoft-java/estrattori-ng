import { HttpClient } from '@angular/common/http';
import { IClientError } from './../global-error-handler/global-error-handler.interfaces';
import { IEventLogger } from './event-logger.interfaces';
import { Observable } from 'rxjs/Observable';
export declare class ServerEventLoggerService implements IEventLogger {
    private http;
    private headers;
    constructor(http: HttpClient);
    logEvent(event: any): Observable<any>;
    logError(error: IClientError): Observable<any>;
}
