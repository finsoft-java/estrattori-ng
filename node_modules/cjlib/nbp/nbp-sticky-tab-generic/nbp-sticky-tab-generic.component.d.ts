import { EventEmitter } from '@angular/core';
import { IStickyTabItemItem } from './nbp-sticky-tab-item.interfaces';
export declare class NbpStickyTabGenericComponent {
    /**
     * headerTab: array degli elementi da visualizzare nei Tab.
     */
    items: IStickyTabItemItem<any>[];
    /**
     * headerTab: Tab selezionato.
     */
    selectedTab: number;
    /**
     * posStickyOn: number. Valore di offset dello scrolling a partire dal quale viene attivato la modalità Sticky
     */
    posStickyOn: number;
    /**
     * tabChange: Evento generato al cambio tab.
     */
    tabChange: EventEmitter<{}>;
    sticky: boolean;
    constructor();
    selectTab(num: number): void;
    onTabChange(idx: number): void;
    onWindowScroll(): void;
}
