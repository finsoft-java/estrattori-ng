import { ElementRef, OnInit } from '@angular/core';
import { AbstractControl, Validator } from '@angular/forms';
import { NbpCurrencyPipe } from './nbp-currency.pipe';
import { NbpPipe } from './../nbp-commons/nbp-commons.enums';
export declare class NbpFormatterDirective implements OnInit, Validator {
    private elementRef;
    private currencyPipe;
    nbpFormatter: NbpPipe;
    private el;
    private initialization;
    private isFormatterSupported;
    constructor(elementRef: ElementRef, currencyPipe: NbpCurrencyPipe);
    validate(control: AbstractControl): {
        [key: string]: any;
    };
    ngOnInit(): void;
    onFocus(value: string): void;
    onBlur(value: string): void;
}
