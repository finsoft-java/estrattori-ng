import { OnInit, SimpleChanges } from '@angular/core';
import { TranslateService } from '@ngx-translate/core';
/**
 * componente per la visualizzazione di una fascia di bottoni a fondo pagina
 */
export declare class NbpBottomPageButtonComponent implements OnInit {
    translate: TranslateService;
    /**
     * array[Object] che contiene la lista dei botton
     * ogni bottone è composto da :
     * (String) name : testo all'interno del bottone
     * (String) icon : classe css che descrive l'icona del bottone
     * (function) action : funzione che viene eseguita al click del bottone
     */
    nbpButtonActionList: object[];
    /**
     * (String) attributo che definisce il titolo della zezione
     */
    nbpTitleBottom: string;
    /**
     * (String) variabile che se definita imposta la larghezza del div che contiene i bottoni
     * in qualsiasi caso non potrà mai avere la larghezza minore di 960px
     */
    nbpFixedWidht: string;
    /**
     * Permette di disabilitare la traduzzione delle voci di menu
     */
    nbpTranslate: boolean;
    constructor(translate: TranslateService);
    _translate(label: string): any;
    rowNumber: number[];
    private countRow(items);
    _buildStyle: (type: any, elem: any) => any;
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
}
