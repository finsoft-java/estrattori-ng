import { Observable } from 'rxjs/Observable';
import { IEmbeddingEvent } from '../nbp-commons/nbp-commons.interface';
import 'rxjs/add/operator/concatMap';
import 'rxjs/add/operator/catch';
import 'rxjs/add/operator/map';
export declare class NbpEmbeddingService {
    private _outgoingEvents$;
    private _incomingEvents$;
    constructor();
    /**
     * Permette di recuperare lo stream degli OutgoingEmbeddingEvents
     */
    getOutgoingEventStream(): Observable<IEmbeddingEvent>;
    /**
     * Permette di recuperare lo stream degli IncomingEmbeddingEvents
     */
    getIncomingEventStream(): Observable<IEmbeddingEvent>;
    /**
     * Permette di aggiungere un embeddingEvent nello stream Outgoing
     * @param event: IEmbeddingEvent
     */
    notifyOutgoingEvent(event: IEmbeddingEvent): void;
    /**
     * Permette di aggiungere un embeddingEvent nello stream Incoming
     * @param event: IEmbeddingEvent
     */
    notifyIncomingEvent(event: IEmbeddingEvent): void;
}
