/**
 * Struttura dati utilizzabile per descrivere le icone
 */
export interface IIconStructure {
    icon?: string;
    hide?: boolean;
    tooltip?: ITooltipStructure;
}
/**
 * Struttura dati utilizzabile per descrivere i tooltip
 */
export interface ITooltipStructure {
    title?: string;
    text: string;
    clickableText?: string;
    onClick?: () => void;
}
export interface IResolutionData {
    width: number;
    height: number;
}
export interface IApplicationInfo {
    application: string;
    version: string;
    environment: string;
}
/**
 * Struttura dati degli eventi POSTMESSAGE scambiati con l'esterno
 */
export interface IEmbeddingMessage {
    eventId: string;
    callerId: string;
    category: string;
    body: any;
}
/**
 * Struttura dati degli EMBEDDING eventi generati dai componenti
 */
/**
 * Struttura dati generica restituita dai servizi REST
 */
export interface ICjRestOutput {
    esito: string;
}
