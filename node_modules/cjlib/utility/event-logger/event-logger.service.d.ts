import { IClientError } from './../global-error-handler/global-error-handler.interfaces';
import { ApplicationStatusService } from './../application-status/application-status.service';
import { ServerEventLoggerService } from './server-event-logger.service';
import { Observable } from 'rxjs/Observable';
/**
 * Servizio istanziato in fase di inizializzazione tramite APP_INITIALIZER
 */
export declare class EventLoggerService {
    private serverLogger;
    _applicationErrors: Observable<IClientError>;
    _applicationEvents: Observable<any>;
    constructor(applicationStatus: ApplicationStatusService, serverLogger: ServerEventLoggerService);
    sendEvent(event: any): void;
    sendError(error: IClientError): void;
}
export declare function ApplicationEventLoggerFactory(startupService: EventLoggerService): Function;
